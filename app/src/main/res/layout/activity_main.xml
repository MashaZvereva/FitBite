<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".presentation.view.MainActivity">

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white"
        android:visibility="gone"
        android:elevation="50dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <com.example.fitbite.presentation.view.ClockView
        android:id="@+id/clockView"
        android:layout_width="300dp"
        android:layout_height="300dp"
        app:layout_constraintBottom_toTopOf="@+id/messageTextView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/messageTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@+id/clockView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="-40dp"
        android:tooltipText="Напоминание о приёмах пищи, согдасно циркадным ритмам"/>

    <LinearLayout
        android:id="@+id/calendarLayout"
        app:layout_constraintTop_toBottomOf="@+id/messageTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="20dp" />

    <!-- Блок с шагами, водой и калориями -->
    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="12dp"
        app:cardElevation="6dp"
        app:layout_constraintTop_toBottomOf="@+id/calendarLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="20dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="16dp"
            android:gravity="center">

            <TextView
                android:id="@+id/stepsTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Шаги: 0"
                android:textSize="18sp"
                android:layout_marginEnd="20dp"
                android:tooltipText="Это всплывающая подсказка!"/>

            <TextView
                android:id="@+id/waterTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Вода: 0 мл"
                android:textSize="18sp"
                android:layout_marginEnd="20dp"
                android:tooltipText="Это всплывающая подсказка!"/>

            <TextView
                android:id="@+id/caloriesTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Калории: 0"
                android:textSize="18sp"
                android:tooltipText="Это всплывающая подсказка!"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Кнопки приёма пищи -->
    <Button
        android:id="@+id/btnBreakfast"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Завтрак"
        app:layout_constraintTop_toBottomOf="@+id/calendarLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.1"
        android:layout_marginTop="100dp"
        style="@style/ButtonWithBorderStyle" /> <!-- Применен стиль для кнопки с рамкой -->

    <Button
        android:id="@+id/btnLunch"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Обед"
        app:layout_constraintTop_toBottomOf="@+id/btnBreakfast"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.1"
        style="@style/ButtonWithBorderStyle" /> <!-- Применен стиль для кнопки с рамкой -->

    <Button
        android:id="@+id/btnDinner"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Ужин"
        app:layout_constraintTop_toBottomOf="@+id/btnLunch"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.1"
        style="@style/ButtonWithBorderStyle" /> <!-- Применен стиль для кнопки с рамкой -->

    <Button
        android:id="@+id/btnSnack"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Перекус"
        app:layout_constraintTop_toBottomOf="@+id/btnDinner"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.1"
        style="@style/ButtonWithBorderStyle" /> <!-- Применен стиль для кнопки с рамкой -->

    <Button
        android:id="@+id/btnActivity"
        style="@style/ButtonWithBorderStyle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Активность"
        app:layout_constraintBottom_toBottomOf="@+id/fragment_container"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/cardView"
        app:layout_constraintVertical_bias="0.1"
        android:layout_marginTop="20dp"/> <!-- Применен стиль для кнопки с рамкой -->


    <!-- Контейнер для кнопок нижнего меню с рамкой -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="6dp"
        app:layout_constraintTop_toBottomOf="@+id/btnSnack"
        tools:layout_editor_absoluteX="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:padding="0dp">

            <Button
                android:id="@+id/btnInfoUser"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="1dp"
                android:layout_weight="1"
                android:background="?android:attr/selectableItemBackground"
                android:text="О Вас"
                android:textSize="14sp" />

            <Button
                android:id="@+id/btnSettings"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="0dp"
                android:layout_weight="1"
                android:background="?android:attr/selectableItemBackground"
                android:maxLines="1"
                android:text="Настрой"
                android:textSize="14sp" />

            <Button
                android:id="@+id/btnFood"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="?android:attr/selectableItemBackground"
                android:text="Рецепты"
                android:textSize="14sp" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>



</androidx.constraintlayout.widget.ConstraintLayout>
